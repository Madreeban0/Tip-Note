spring.application.name=TipNoteMaker

# --- PRODUCTION DATABASE CONFIGURATION ---
# These values are injected by Render's Environment Variables
# Spring Boot automatically converts SPRING_DATASOURCE_URL to spring.datasource.url
spring.datasource.url=jdbc:postgresql://dpg-d40pbo95pdvs73dab3n0-a/notesdb_vfqk
spring.datasource.username=notesdb_vfqk_user
spring.datasource.password=CG9oTgFJp2sZ9F3OPeGZOT6kJB7Ctjsg

# --- PRODUCTION JPA/HIBERNATE SETTINGS ---

# 'validate' is much safer for production than 'update'.
# 'update' can be slow and risks data loss if a change is destructive.
# Use 'validate' to confirm your Java entities match the database schema.
spring.jpa.hibernate.ddl-auto=validate

# Set to 'false' for production to avoid noisy logs.
spring.jpa.show-sql=false

# This is correct and helps Hibernate use the right SQL syntax.
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
